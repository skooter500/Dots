# light-locker
# screen locker for lightdm
light-locker&

# swap caps lock and left control
setxkbmap -option 'ctrl:swapcaps'

# typematic delay
# xset r rate <initial-delay (ms)> <repeat-freq (Hz)>
xset r rate 500 40
