# Environment variables
# Syntax defined in pam_env.conf (5) man page
# (Note: End of line comments are not allowed)

# xdg base directories
XDG_CACHE_HOME  DEFAULT="/home/mike/.cache"
XDG_CONFIG_HOME DEFAULT="/home/mike/.config"
XDG_DATA_HOME   DEFAULT="/home/mike/.local/share"

# zsh
ZDOTDIR     DEFAULT="/home/mike/.zsh"

# default applications
BROWSER     DEFAULT="firefox"
EDITOR      DEFAULT="emacs -nw"
PAGER       DEFAULT="less"
VISUAL      DEFAULT="emacs"

# enable touchscreen scrolling in firefox
MOZ_USE_XINPUT2     DEFAULT=1

# window opacity, for use with compton
OPACITY     DEFAULT=0.85

# less
LESS            DEFAULT="-R"
LESSOPEN        DEFAULT="| /usr/bin/src-hilite-lesspipe.sh %s"
LESSHISTFILE    DEFAULT="/dev/null"
